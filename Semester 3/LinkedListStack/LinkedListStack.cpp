#ifdef LLS_DEBUG_CODE

#include <iostream>
#include "LinkedListStack.h"

using namespace LLS;

int main()
{
    // test code
    LinkedListStack<int> stacklist;
    stacklist.push_back(0);
    stacklist.push_back(1);
    stacklist.push_back(2);
    stacklist.push_back(3);
}

#endif